# Shred 20

A 20-day calorie deficit tracker. Goal: 21,000 cal net deficit (~1,050/day, ~6 lbs).

Single-file HTML app. Data persists locally via `localStorage` + IndexedDB. Export/import JSON backups.

## Run
Open `index.html` in any browser, or visit the GitHub Pages site.
